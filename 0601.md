# 周报 | Weekly Report

---

## 基本信息

- **姓名：** 邱张西子
- **日期：** 2026-06-01

---

## 1. 研究领域

基于预训练视频扩散模型的个性化视频生成，具体方向为将 Test-Time Training（TTT）机制引入 Wan2.1 视频 DiT，实现推理时对特定主体的快速个性化适配（Test-Time Personalization）。

---

## 2. 本周工作

### 2.1 Test-Time Personalization 框架设计与实现

明确了研究方向：不同于离线 DreamBooth/LoRA 微调，本工作的核心贡献是推理时个性化——给定用户提供的 3-5 张参考图，在推理前对模型做快速适配，使模型"记住"该主体后再生成视频。

实现了三个新模块：

- **ttt_personalize.py**：Test-Time Adaptation 推理脚本。给定参考图，在推理前对 W1/b1（TTT 内部权重）和 ref_proj 做若干步梯度更新，然后再执行标准去噪采样。支持 `--adapt_steps 0` 跳过适配作为 baseline 对比。
- **ttt/datasets/episode_dataset.py**：Stage 2 meta-learning 数据集。每个 episode 包含 support（同语义组的参考图 CLIP embedding）和 query（不同视频的 VAE latent + text embed），用于 MAML 风格的 meta-training。
- **train_meta.py**：Stage 2 MAML-style meta-training 脚本。inner loop 在 support 上更新 W1/b1，outer loop 在 query 上计算 flow matching loss，支持 FOMAML（first-order）加速。

### 2.2 训练基础设施调试

在服务器（A100 40GB）上启动训练时遇到多个问题并逐一修复：

**问题 1：第一步 backward 极慢（>20 分钟）**

原因：`mini_batch_size=30` 导致 TTT scan 有 585 个 mini-batch，Python for 循环串行执行，backward 时 gradient checkpointing 需重算所有 mini-batch，单步耗时约 20 分钟。

修复：将 `mini_batch_size` 从 30 改为 255，mini-batch 数量从 585 降至 10，同时关闭 `scan_checkpoint_group_size`（设为 0），每步耗时降至约 2 秒。

**问题 2：ref_proj 学会忽略 ref 信号**

原因：训练时 `cross_ref=True`，ref 来自不同视频，与 query video 无语义关联，模型最优策略是将 ref token 的 attention weight 压向零。实测 `ref_proj.weight` Frobenius norm 仅 22.6（随机初始化期望约 64），说明 ref_proj 被抑制。

修复：
1. `cross_ref=False`：改用 same-video ref，让模型学会"看参考图 → 生成对应外观"
2. 新增 `ref_norm`（LayerNorm）：对 ref_proj 输出做归一化，使 ref token 的 norm 与 T5 token 对齐，避免被稀释
3. `lr_ref_proj=3e-4`：ref_proj 学习率提高至 TTT 的 3 倍，加快收敛

### 2.3 多轮训练实验

| 版本 | 步数 | 关键配置 | 问题 |
|------|------|----------|------|
| v2 | 500 | mini_batch=255, cross_ref=True | ref 无效果 |
| v3 | 3000 | resume from v2, lr_ref=3e-4 | ref 仍无效果（cross_ref 未改） |
| v4 | 3000 | cross_ref=False, ref_norm, lr_ref=3e-4 | 当前最新，效果待评估 |

### 2.4 推理对比实验问题定位

固定 `seed=42` 和相同 prompt 后，对比 base / no-ref / with-ref 三组结果，发现 with-ref 和 no-ref 几乎无差异。结合权重分析确认根本原因为 ref_proj 被训练成忽略 ref 信号（cross-video ref sampling 导致），而非模型架构问题。

### 2.5 Stage 2 Meta-Training 启动

Stage 1（v4）完成后自动触发 Stage 2。修复了 `train_meta.py` 中 `query_text_embed` 未移至 GPU 的 device mismatch bug，Stage 2 目前正在运行中（996 episodes，26 subject groups，3000 步）。

---

## 3. 结论与发现

1. **TTT scan 速度是训练瓶颈**：`mini_batch_size` 对训练速度影响极大，从 30 改为 255 后每步从 20 分钟降至 2 秒，是本周最关键的工程优化。
2. **cross-video ref sampling 是 ref 失效的根本原因**：训练时 ref 与 query 无关联，模型学会忽略 ref 是理性选择。same-video ref 是正确的训练范式。
3. **ref token 的 norm 对齐至关重要**：ref_proj 输出不加 LayerNorm 时，ref token 在 cross-attention 中被 512 个 T5 token 稀释，加 LayerNorm 后 ref token 与 text token 处于同一量级。
4. **MAML-style meta-training 的必要性**：Stage 1 训练的是通用视觉对齐能力，Stage 2 的 inner/outer loop 结构才能让模型学会"快速适配新主体"的元能力，是 test-time personalization 的核心。

---

## 4. 下周计划

- [ ] 评估 v4 checkpoint 的推理效果，对比 with-ref vs no-ref（固定 seed）
- [ ] 等待 Stage 2 完成，用 Stage 2 checkpoint 测试 `ttt_personalize.py` 的 test-time adaptation 效果
- [ ] 下载 DreamBench 数据集，计算 DINO-I / CLIP-T / CLIP-I 量化指标
- [ ] 调研 patch-level CLIP features（196 patch tokens）替代 pooler output，提升细粒度外观保真度
- [ ] 整理实验结果，撰写方法部分初稿

---

## 附录

### 当前训练状态

| 项目 | 数值 |
|------|------|
| 数据集 | MSR-VTT 1000 clips |
| 分辨率 | 272×480，1s@16fps |
| Stage 1 v4 步数 | 3000 / 3000 ✓ |
| Stage 1 v4 最终 loss | ~0.093 |
| Stage 2 状态 | 运行中 |
| GPU | A100 40GB |
| Delta checkpoint 大小 | ~580MB |

### 关键超参数（v4）

```
cross_ref = false

[meta]
inner_steps = 3
inner_lr = 1e-3
first_order = true
n_support = 3
```

### 参考文献

1. Sun, Y., et al. *Learning to (Learn at Test Time): RNNs with Expressive Hidden States.* arXiv:2407.04620, 2024.
2. Dalal, G., et al. *Test-Time Training on Video with TTT-Linear.* (ttt-video-dit), 2024.
3. Wan Team (Alibaba). *Wan: Open and Advanced Large-Scale Video Generative Models.* 2025.
4. Finn, C., et al. *Model-Agnostic Meta-Learning for Fast Adaptation of Deep Networks.* ICML 2017.
5. Ye, H., et al. *IP-Adapter: Text Compatible Image Prompt Adapter for Text-to-Image Diffusion Models.* arXiv:2308.06721, 2023.
