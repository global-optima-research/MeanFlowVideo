# 周报 | Weekly Report

---

## 基本信息

- **姓名：** 邱张西子
- **日期：** 2026-06-15

---

## 1. 研究领域

基于预训练视频扩散模型的个性化视频生成，具体方向为将 **Test-Time Training（TTT）** 机制引入 Wan2.1 视频 DiT，使模型在推理时具备对特定主体的外观适应能力，结合 ref_proj 参考图注入实现主体个性化生成。

---

## 2. 本周工作

### 2.1 服务器迁移与环境配置

项目从 `/root/` 环境迁移至用户空间 `/data/qiuzhangxizi/`，重新配置推理环境：

- 确认 conda 环境 `wan`（python 3.12）可用
- 发现并解决 umt5-xxl tokenizer 缺失问题：通过 ModelScope 下载 `google/umt5-xxl` tokenizer 文件，软链接至 `wan2.1-1.3B/google/umt5-xxl`
- 安装缺失依赖 `sentencepiece`

### 2.2 Stage1 / Stage2 训练结果确认

确认两阶段训练已完成：

- **Stage1**：episode-based meta-training，最终 checkpoint `step_004950.pt`（557MB），`step_005000.pt` 文件损坏（420MB，zip 读取失败）
- **Stage2**：meta-learning，`final.pt` 及每 300 步 checkpoint，训练 loss 收敛至 0.08～0.20 区间，3000 步完成

| Stage | 最终步数 | Loss 区间 | 最终 checkpoint |
|-------|---------|-----------|----------------|
| Stage1 | 4950 步（5000 损坏） | 0.08～0.30 | step_004950.pt（557MB） |
| Stage2 | 3000 步 | 0.08～0.50 | final.pt |

### 2.3 推理调试与 Bug 修复

#### Bug：CFG null 路传入 ref_embeds（主因）

在 `infer_with_ref.py` 的 sampling loop 中，CFG negative prompt 那路也传入了 `ref_embeds`：

```python
# 修复前（错误）
nu = model(latents, t=ts, context=context_null, seq_len=seq_len,
           ref_embeds=ref_embeds)[0]

# 修复后（正确）
nu = model(latents, t=ts, context=context_null, seq_len=seq_len)[0]
```

CFG 的原理是 `pred = null + scale * (cond - null)`，null 路也传 ref 导致相减后 ref 信息被大幅抵消，模型生成结果接近无条件生成。修复后 ref 信号可以正常生效。

### 2.4 推理对比实验

使用 prompt **"a dog running in the park"**，参考图 `ref_dog6.jpg`（柯基幼犬），seed=42，对以下四组进行对比：

| 组别 | checkpoint | ref | cfg_scale | 结论 |
|------|-----------|-----|-----------|------|
| ① baseline | 无 | 无 | 5.0 | 有狗的形象，画质偏低 |
| ② baseline | 无 | 无 | 7.5 | 画质明显提升，推荐值 |
| ③ Stage1（fix） | step_004950 | ✓ | 5.0 | 主体形象混乱 |
| ④ Stage2（fix） | final.pt | ✓ | 5.0 | 主体形象混乱 |
| ⑤ Stage2，无 ref | final.pt | 无 | 7.5 | 有柴犬外观倾向 |

关键发现：Stage2 在**不传 ref 的情况下**已经能生成偏向柴犬的形象，说明 TTT 权重从 MSR-VTT 数据中学到了一定的狗的外观偏好；但加入 ref 后结果反而变差，指向 ref_proj 本身未训练好。

### 2.5 问题定位：ref_proj 权重被 weight decay 抑制

对 stage1/stage2 checkpoint 中 ref_proj 权重进行分析：

```
stage1 ref_proj.weight: std=0.0181  norm=22.625
stage2 ref_proj.weight: std=0.0181  norm=22.625  ← stage2 完全未更新
```

- weight norm 仅 22.6，远低于随机初始化期望值（~64.0）
- stage1 和 stage2 权重几乎完全相同，说明 stage2 训练过程中 ref_proj 没有得到有效梯度更新
- TTT gate 值极小（`tanh(gate)` 最大 0.045），TTT 残差贡献接近零，模型退化为近似 base model

**根本原因：** `train.py` 中所有 param group 共用 `weight_decay=0.01`，ref_proj 虽有独立 lr group 但未单独关闭 weight decay，持续被压向零。

### 2.6 修复：ref_proj 关闭 weight decay

修改 `train.py` optimizer 配置，为 ref_proj param group 单独设置 `weight_decay=0.0`：

```python
# 修复前
param_groups = [{"params": ttt_params, "lr": tcfg["lr_ttt"]},
                {"params": ref_params,  "lr": lr_ref}]
optimizer = torch.optim.AdamW(param_groups, weight_decay=tcfg["weight_decay"])

# 修复后
param_groups = [{"params": ttt_params, "lr": tcfg["lr_ttt"]},
                {"params": ref_params,  "lr": lr_ref, "weight_decay": 0.0}]
optimizer = torch.optim.AdamW(param_groups, weight_decay=tcfg["weight_decay"])
```

### 2.7 数据集质量分析

对 MSR-VTT 训练数据进行初步分析，发现两个问题：

1. **subject_group 标注不准**：`subject_group: dog` 的视频中，实际内容不一定包含狗（如 caption 为"a man describes canine injuries"被标为 dog 组）
2. **视频分辨率低**：MSR-VTT 原始视频约 240p，训练数据质量本身偏低，是生成视频油画感的部分原因

---

## 3. 结论与发现

1. **CFG null 路不应传 ref_embeds**：这是导致前期推理结果混乱的直接原因，修复后 ref 信号不再被 CFG 相减抵消。

2. **ref_proj weight decay 是核心训练 Bug**：ref_proj 权重被持续压缩至接近零，导致参考图注入无效。修复方案（`weight_decay=0.0`）已提交至 `train.py`，需要重新训练验证。

3. **TTT gate 值极小，模型退化为 base model**：TTT 机制实际上没有发挥作用，gate 始终接近零。这可能与 loss landscape 有关——base model 损失已经较低，TTT 残差的梯度信号不足以驱动 gate 增大。

4. **数据质量是下一个瓶颈**：MSR-VTT 分辨率低、标注噪声大，即使 ref_proj 修复后，数据质量也会限制个性化效果的上限。

---

## 4. 下周计划

- [ ] 用修复后的 `train.py`（ref_proj weight_decay=0）重新跑 Stage1 训练，验证 ref_proj 权重是否正常增长
- [ ] 清洗 MSR-VTT subject_group 标注：用 CLIP 相似度过滤 caption 与 subject_group 不匹配的样本
- [ ] 调研 Panda-70M 数据集接入方案，替换 MSR-VTT 作为训练数据
- [ ] 分析 TTT gate 值过小的问题，考虑对 gate 初始化或梯度流进行调整

---

## 附录

### 当前训练状态

| 项目 | 数值 |
|------|------|
| 数据集 | MSR-VTT 1000 clips |
| 分辨率 | 272×480，1s@16fps |
| Stage1 最终步数 | 4950 / 5000（5000 损坏） |
| Stage2 最终步数 | 3000 / 3000 ✓ |
| GPU | qiuzhangxizi 服务器（111.17.197.107） |
| 已保存 checkpoints | stage1: step_004800～004950；stage2: step_000300～003000，final.pt |

### 关键超参数

```toml
# Stage1
[training]
num_steps = 5000
lr_ttt = 1e-4
lr_ref_proj = 3e-4
weight_decay = 0.01  # bug: ref_proj 未单独关闭，已修复

# Stage2
[meta]
n_support = 3
inner_steps = 3
inner_lr = 1e-3
first_order = true
```

### 参考文献

1. Sun, Y., et al. *Learning to (Learn at Test Time): RNNs with Expressive Hidden States.* arXiv:2407.04620, 2024.
2. Dalal, G., et al. *Test-Time Training on Video with TTT-Linear.* (ttt-video-dit), 2024.
3. Wan Team (Alibaba). *Wan: Open and Advanced Large-Scale Video Generative Models.* 2025.
4. Ruiz, N., et al. *DreamBooth: Fine Tuning Text-to-Image Diffusion Models for Subject-Driven Generation.* CVPR 2023.
5. Ye, H., et al. *IP-Adapter: Text Compatible Image Prompt Adapter for Text-to-Image Diffusion Models.* arXiv:2308.06721, 2023.
