# 周报 | Weekly Report

---

## 基本信息

- **姓名：** QIU Zhangxizi
- **日期：** 2026-05-11

---

## 1. 研究领域

基于预训练视频扩散模型的个性化视频生成，具体方向为将 **Test-Time Training（TTT）** 机制引入 Wan2.1 视频 DiT，使模型在推理/微调时具备动态更新内部状态的能力，从而提升对特定主体/场景的适应性。

---

## 2. 领域核心问题

### 背景

近年来视频扩散模型（如 Wan2.1、CogVideoX、HunyuanVideo）在通用文生视频上取得了显著进展，但在**个性化视频生成**（Personalized / Subject-driven Video Generation）上仍面临核心矛盾：

1. **静态权重的表达局限**：Transformer 的自注意力机制在处理长序列视频 token 时，无法在 forward pass 中动态更新权重，对新主体的适应能力受限。
2. **微调代价高**：全量 SFT 代价大，LoRA 等参数高效方法在时序一致性上效果有限。
3. **序列建模能力不足**：标准自注意力的复杂度为 $O(L^2)$，对长视频 token 序列（典型值 13×30×45 ≈ 17,550 tokens）压力大；同时缺乏显式的跨帧状态传播机制。

### 核心问题

**如何在保留预训练 Wan2.1 视频扩散先验的前提下，通过引入可在 test-time 自适应更新的序列层，提升个性化视频生成的时序一致性与细节保真度？**

---

## 3. 技术方案

### 3.1 整体思路

将 **TTT-Linear**（Test-Time Training Linear Layer）以**并联残差**的方式插入 Wan2.1 的每个 Transformer block，构成 **WanModelTTT**。TTT 层在 forward pass 中通过对当前输入做一步梯度下降来更新自身内部权重矩阵（$W_1$），从而实现序列内的动态状态更新，弥补纯注意力机制在长程状态传播上的不足。

### 3.2 参考工作

| 工作 | 贡献 |
|------|------|
| **Test-Time Training on Video with TTT-Linear** (TTT-Video, 2024) | 首次将 TTT-Linear 引入视频 DiT，验证了 TTT 层可提升视频生成质量 |
| **Wan2.1** (Alibaba, 2025) | 开源 1.3B 视频 DiT，支持文生视频，采用 3D RoPE + v-prediction 训练目标 |
| **Learning to (Learn at Test Time): RNNs with Expressive Hidden States** (Sun et al., 2024) | TTT 机制原始提案：将模型权重作为隐状态，通过自监督梯度下降更新 |
| **CogVideoX** (Zheng et al., 2024) | 采用余弦噪声调度 + v-prediction 的视频扩散框架（训练目标参考） |
| **DreamBooth / CustomDiffusion** | 个性化图像生成范式，本方案的对比基线方向 |

### 3.3 模型结构：WanModelTTT

在原始 Wan2.1 的每个 `WanTransformerBlock` 内，在自注意力（Self-Attention）之后、Cross-Attention 之前，插入一个 **TTT-Linear 残差分支**：

```
x  ─→ norm1 ─→ AdaLN ─→ Self-Attn ─→ + ─→ norm_ttt ─→ TTT-Linear ─→ tanh(gate) ─→ + ─→ Cross-Attn ─→ + ─→ FFN ─→ +
│                                      ↑                                              ↑                   ↑          ↑
└──────────────────────────────────────┘              ─────────────────────────────────┘                  │          │
                                                                                                           └──────────┘
```

**关键设计细节：**

- **TTT Gate 初始化**：`ttt_gate` 参数初始化为 `0.1`，经 `tanh` 激活后输出约 `0.1`，确保新插入的 TTT 层在训练初期对原始预训练分布影响极小，避免破坏 Wan2.1 已有的视频生成能力。
- **Gradient Checkpointing**：训练时对每个 Transformer block 使用 `torch.utils.checkpoint`，大幅降低显存占用，代价是约 30% 的速度损失。

### 3.4 TTT-Linear 层详解

**核心直觉**：自注意力中 $\text{softmax}(QK^\top)V$ 是对序列的线性插值；TTT 则把 $W_1$ 看作隐状态，通过每个 mini-batch 上的一步梯度下降来"学习"当前 token 的上下文。

**Mini-batch 扫描（Scan over NC）：**

# Mini-batch 扫描（Scan over NC）

## 1. 定义与符号
序列长度 $L$ 按 mini-batch 分块：
$$
N_C = \lceil \frac{L}{\text{mini\_batch\_size}} \rceil
$$

- $X_Q$：Query 块
- $X_K$：Key 块
- $X_V$：Value 块
- $W_1,b_1$：投影权重与偏置
- $\bar{b}_1$：修正偏置
- $\text{LN}(\cdot)$：LayerNorm
- $\mathcal{L}_\text{LN-L2}$：带 L2 正则的归一化损失
- $\eta$：步长
- $\eta_\text{last}$：末轮学习率

## 2. 逐块计算流程

### （1）Key 侧投影
$$
Z_1 = X_K W_1 + b_1
$$

### （2）损失梯度
$$
\text{grad} = \nabla_{Z_1} \mathcal{L}_\text{LN-L2}\bigl(Z_1,\ X_V - X_K\bigr)
$$

### （3）Query 侧修正投影
$$
\bar{Z}_1 = X_Q W_1 - \eta \cdot \left(X_Q X_K^\top\right) \text{grad} + \bar{b}_1
$$

### （4）残差输出
$$
\text{out} = X_Q + \text{LN}\bigl(\bar{Z}_1\bigr)
$$

### （5）在线更新权重
$$
W_1^{(t+1)} = W_1^{(t)} - \eta_\text{last} \cdot X_K^\top \text{grad}
$$

## 3. 说明
- 长序列分块扫描，降低显存占用。
- 采用在线梯度修正，提升训练稳定性。
- 残差结构保证梯度回传通畅。

### 3.5 位置编码：3D RoPE

Wan2.1 采用 3D RoPE，将 head dimension 按 $\frac{1}{4}:\frac{3}{8}:\frac{3}{8}$ 的比例分配给时间轴 $t$、高度轴 $h$、宽度轴 $w$：

```
head_dim = 128 (= 1536 / 12)
  d_t = 32   → temporal freqs
  d_h = 48   → height freqs
  d_w = 48   → width freqs
```

TTT 层的 $X_Q, X_K$ 同样施加相同的 3D RoPE，保证位置感知一致性。

### 3.6 噪声调度与训练目标

采用 **v-prediction**（velocity parameterization）+余弦噪声调度：

$$
x_t = \sqrt{\bar\alpha_t}\, x_0 + \sqrt{1-\bar\alpha_t}\, \epsilon
$$
$$
v_t = \sqrt{\bar\alpha_t}\, \epsilon - \sqrt{1-\bar\alpha_t}\, x_0
$$

模型预测 $v_t$，损失为：

$$
\mathcal{L} = \mathbb{E}_{x_0, \epsilon, t} \left[\|f_\theta(x_t, t, c) - v_t\|^2\right]
$$

### 3.7 Optimizer 策略

TTT 层参数使用**更高学习率**（`lr_ttt = 1e-4`），而预训练 Wan2.1 参数使用较低学习率（`lr_base = 1e-5`），防止预训练权重被过度扰动：

```python
optimizer = AdamW([
    {"params": base_params, "lr": 1e-5},
    {"params": ttt_params,  "lr": 1e-4},
], weight_decay=0.01)
```

配合 warmup（100 steps）+ 线性 decay 的 LR schedule。

### 3.8 权重加载策略

从 Wan2.1 官方 safetensors checkpoint 加载预训练权重，需处理以下 key 映射差异：

| checkpoint key | model key | 说明 |
|---|---|---|
| `blocks.i.ffn.0.*` | `blocks.i.ffn.gate_up.*` | Wan 原始 FFN 无 gate；模型改为 SiLU-GLU，加载时将 up-proj 复制为 gate+up |
| `blocks.i.ffn.2.*` | `blocks.i.ffn.down.*` | 直接重命名 |
| `head.head.*` | `head.*` | 去掉嵌套层名 |
| `head.modulation` | `head_modulation` | 提升到顶层 |
| TTT 相关 keys | — | 随机初始化，不从 checkpoint 加载 |

### 3.9 数据格式

训练数据以 JSONL 格式组织，支持两种模式：

- **Mode A（推荐）**：预编码 VAE latents（`.pt` 格式，shape `(C, T, H, W)`）+ 预编码 T5 embeddings，训练速度快。
- **Mode B**：原始 MP4 + 文本 caption，运行时调用 VAE/T5 编码，灵活但慢。

---

## 4. 本周工作

- [x] 完成 `WanModelTTT` 模型结构设计与实现（`ttt/models/wan/model.py`），包括 AdaLN 调制、SiLU-GLU FFN、TTT 残差分支
- [x] 实现 `TTTLinear` 层（`ttt/models/ssm/ttt_layer.py`），包含 mini-batch scan、per-token 动态 LR、3D RoPE、LN-fused L2 梯度计算
- [x] 实现 Wan2.1 预训练权重加载逻辑（`ttt/models/wan/load_weights.py`），处理 FFN key 映射与 head key 重命名
- [x] 实现 v-prediction DDPMScheduler（`ttt/infra/noise_scheduler.py`），采用余弦噪声调度
- [x] 实现 `VideoDataset`，支持预编码 latent 和原始视频两种输入模式
- [x] 完成 Stage 1 全量 SFT 训练脚本（`train.py`），支持 DDP 多卡训练、梯度累积、TTT 分组学习率
- [x] 配置 `configs/train_stage1.toml`，完成超参数设定（5000 steps，bfloat16，480×720，3s@16fps）
- [ ] 准备训练数据（JSONL + latents），**正在进行**
- [ ] 启动 Stage 1 训练，监控 loss 曲线

---

## 5. 结论与发现

1. **TTT Gate 初始化很关键**：将 `ttt_gate` 初始化为 `0.1`（经 `tanh` 激活 ≈ 0.1），而非直接初始化为 1，可以避免在训练初期 TTT 输出过大破坏预训练权重的分布。
2. **FFN key 映射踩坑**：Wan2.1 官方 checkpoint 的 FFN 是普通 up-proj（无 gate），而为了对齐现代 DiT 的 SiLU-GLU 设计，模型改为 GLU；加载时需将 up-proj 权重复制为 gate 和 up 两份拼接，否则维度不匹配。
3. **序列长度整除约束**：TTT-Linear 要求序列长度 $L$ 整除 `mini_batch_size`。480×720 视频在 patch_size=(1,2,2) 下得到 13×30×45=17,550 tokens，选 `mini_batch_size=30` 恰好整除（$17550/30=585$ 个 mini-batch）。
4. **显存压力**：在 1.3B 参数模型 + bfloat16 + TTT 内部状态下，单卡显存需求约 40GB+；通过 block-level gradient checkpointing + TTT scan checkpointing 可降至可接受范围。

---

## 6. 下周计划

- [ ] 完成训练数据准备（预编码 latents + T5 embeddings）并启动 Stage 1 训练
- [ ] 监控训练 loss，确认 TTT 层是否正常收敛（预期 TTT params loss 下降应快于 base params）
- [ ] 实现推理脚本（inference pipeline：DDIM/DDPM 采样 + VAE decode）
- [ ] 对比实验：TTT-off（`ttt_gate=0`）vs TTT-on，验证 TTT 层的实际效果
- [ ] 调研 Stage 2 方案：冻结 base params，仅微调 TTT 层，用于快速个性化适配

---

## 附录

### 模型规模

| 组件 | 参数量 |
|------|--------|
| Wan2.1 base（dim=1536, 30 layers） | ~1.3B |
| TTT 新增参数（per block: W1, b1, wq/k/v/o, lr_weight, norms, gate） | ~每层约 37M，共 ~110M |
| TTT 占总参数比 | ~7-8% |

### 关键超参数

```toml
[model]
dim = 1536, num_heads = 12, num_layers = 30, ffn_dim = 8960
latent: 480×720 → 13×30×45 = 17,550 tokens (post patch)

[ttt]
mini_batch_size = 30   # L=17550, NC=585
base_lr = 1.0
scan_checkpoint_group_size = 4

[training]
num_steps = 5000, warmup = 100
lr_base = 1e-5, lr_ttt = 1e-4
dtype = bfloat16, grad_accum = 4
```

### 参考文献

1. Sun, Y., et al. *Learning to (Learn at Test Time): RNNs with Expressive Hidden States.* arXiv:2407.04620, 2024.
2. Dalal, G., et al. *Test-Time Training on Video with TTT-Linear.* (ttt-video-dit), 2024.
3. Wan Team (Alibaba). *Wan: Open and Advanced Large-Scale Video Generative Models.* 2025.
4. Yang, Z., et al. *CogVideoX: Text-to-Video Diffusion Models with An Expert Transformer.* arXiv:2408.06072, 2024.
5. Ho, J., et al. *Denoising Diffusion Probabilistic Models.* NeurIPS 2020.
6. Salimans, T. & Ho, J. *Progressive Distillation for Fast Sampling of Diffusion Models.* ICLR 2022. （v-prediction 参数化）
7. Ruiz, N., et al. *DreamBooth: Fine Tuning Text-to-Image Diffusion Models for Subject-Driven Generation.* CVPR 2023.
