Train Model Teacher: Wan2.1 T2V 1.3B 

step1 质量差的原因：
1. 平滑效应：当模型尝试在 1 步内预测出原本需要 50 步演化的结果时，为了保证能够“覆盖”所有可能的数据点（多样性），模型会倾向于输出一个统计平均值。
2. 任何微小的预测偏差都会在 1-step 中被无限放大。因为没有后续步数（Step 2, 3...）来修正路径，模型会掉入预训练分布之外的“无效区域”。
3. 在计算 MeanFlow 所需的切线方向（JVP）时，BF16 的舍入误差在大参数量下会产生巨大的数值噪声