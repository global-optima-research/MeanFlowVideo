# 周报 | Weekly Report

---

## 基本信息

- **姓名：** 邱张西子
- **日期：** 2026-05-18

---

## 1. 研究领域

基于预训练视频扩散模型的个性化视频生成，具体方向为将 **Test-Time Training（TTT）** 机制引入 Wan2.1 视频 DiT，使模型在推理/微调时具备动态更新内部状态的能力，从而提升对特定主体/场景的适应性。

---

## 2. 本周工作

### 2.1 扩展 precompute pipeline：CLIP 参考帧编码

在上周已实现的 VAE latent 编码和 T5 文本编码基础上，本周新增了 **CLIP 参考帧编码**模块（`ttt/infra/precompute.py`，`--mode ref_image`）：

- 从每个视频中以均匀间隔抽取 `num_ref_frames=4` 帧作为参考帧，模拟推理时用户提供的参考图像
- 使用 `openai/clip-vit-large-patch14` 的 vision encoder 提取 `pooler_output`，得到每视频 shape `(4, 1024)` 的参考帧 embedding
- 更新 `dataset.jsonl`，加入 `ref_emb` 字段，形成完整的三元组数据格式：

```json
{"latent": "video0.pt", "text_embed": "video0_text.pt", "ref_emb": "video0_ref.pt"}
```

### 2.2 WanModelTTT 扩展：参考图注入机制

在 `WanModelTTT` 中新增 `ref_proj` 线性层（`nn.Linear(1024, dim)`），将 CLIP 1024d embedding 投影到模型的 context 维度，并拼接在 T5 文本 token 之后，一起送入每层的 Cross-Attention：

```python
# WanModelTTT.forward()
if ref_embeds is not None:
    ref_tokens = self.ref_proj(ref_embeds)       # (B, N_ref, dim)
    context = torch.cat([context, ref_tokens], dim=1)
```

训练时 `ref_proj` 与 TTT 层共同参与梯度更新（`lr = 1e-4`），其余 Wan2.1 参数冻结。

### 2.3 MSR-VTT 数据集准备与完整 pipeline 运行

下载并处理了 **MSR-VTT** 数据集的 1000 条视频样本：

- 编写 `scripts/download_msrvtt.py`，从 HuggingFace（`AlexZigma/msr-vtt`）下载视频 zip（4.3GB）和 caption parquet，提取 1000 个 mp4 并生成 `captions.jsonl`
- 依次完成三阶段预编码：
  - **VAE 编码**：1000 个视频，latent shape `(16, 5, 34, 60)`（分辨率 272×480，时长缩短至 1 秒以加速训练）
  - **T5 编码**：1000 条 caption → `*_text.pt`，shape `(L, 4096)`
  - **CLIP 编码**：每视频 4 帧参考图 → `*_ref.pt`，shape `(4, 1024)`

### 2.4 训练配置调优与正式训练启动

针对 A100 40GB 显卡进行了以下调优：

| 参数 | 初始值 | 最终值 | 原因 |
|------|--------|--------|------|
| `video_length_sec` | 3s | **1s** | seq_len 从 6630 降至 2550，训练速度提升 ~5× |
| `global_batch_size` | 2 | **8** | 显存从 17GB 提升至 28.6GB（70% 利用率），batch=16 OOM |
| `grad_accum_steps` | 4 | **1** | 配合 batch_size=8，有效 batch 不变 |

在 MSR-VTT 1000 条样本上启动 5000 步训练，每 500 步保存一次 delta checkpoint（仅保存 TTT + ref_proj 参数，~557MB，相比全量 3.2GB 节省 83%）。当前进度：**step 2090/5000**，loss 稳定在 0.13~0.30。

**训练配置：**
```toml
[training]
video_length_sec = 1
fps = 16
resolution = [272, 480]      # seq_len = 2550
global_batch_size = 8
num_steps = 5000
warmup_steps = 200
lr_ttt = 1e-4
lr_base = 1e-5
```

### 2.5 推理对比实验

#### 实验一：base model vs. finetuned（step 1500）

使用相同 prompt「a dog running on the beach」、相同 seed=42，对比原始 Wan2.1 与 step 1500 checkpoint 的生成效果：

- **base model**：生成质量正常，符合 Wan2.1 预训练水平
- **finetuned（step 1500）**：主观上画面内容更清晰、运动更自然

分析：当前阶段的提升主要来自 **MSR-VTT 数据 finetune 带来的分布对齐**，而非 TTT 个性化机制——训练时 TTT gate 从零初始化，早期阶段 TTT 的实际贡献接近零，ref_proj 也仍在学习 CLIP 特征空间到 Wan context 空间的对齐。

#### 实验二：有无参考图对比（step 2000）

实现 `infer_with_ref.py`，支持输入任意参考图像（CLIP 编码后经 `ref_proj` 注入 context），并与无参考图推理进行对比。

使用 DreamBooth dog6 数据集中的参考图（`00.jpg`），同一 prompt 和 seed：

- **无参考图**：正常生成"狗在沙滩奔跑"
- **有参考图**：生成内容在外观/品种细节上向参考图靠拢

当前效果受限于 `ref_proj` 的训练步数（2000 步），随着训练推进预计对齐效果进一步提升。

---

## 3. 结论与发现

1. **视频长度对训练速度影响显著**：seq_len 与 attention 计算量成平方关系，从 3s（seq_len=6630）缩短到 1s（seq_len=2550）后，训练速度提升约 5 倍，1 秒视频足以学习基本的运动-文本对应关系。

2. **Delta checkpoint 策略有效**：仅保存 TTT + ref_proj 参数（~557MB），相比全量权重（3.2GB）节省 83%，且加载时通过 `strict=False` 与 base model 合并，推理流程完整。

3. **TTT gate 的两面性**：gate 初始为零保证了训练稳定性，但也使得 TTT 层在训练早期贡献极小。真正的 TTT 个性化潜力（推理时对 W 矩阵做 in-context 更新）尚未激活，需要专门设计推理时的 W pre-warming 机制。

4. **ref_proj 是当前个性化能力的关键**：CLIP embedding 注入 context 的方案在原理上可行，当前已能在生成结果中观察到参考图的语义影响，但与 DreamBooth 级别的外观保真度仍有差距，后者需要更多训练步数或专门的 subject-driven 损失。

---

## 4. 下周计划

- [ ] 等待训练完成（step 5000），用最终 checkpoint 重跑对比实验
- [ ] 实现推理时 TTT W pre-warming：给定参考图后，在推理前对 TTT 内部 W 矩阵做若干步梯度更新，激活真正的 test-time 个性化机制
- [ ] 下载 DreamBench 30 个主体数据集，编写 `eval.py` 计算 DINO-I / CLIP-T / CLIP-I 量化指标
- [ ] 调研更强的参考图注入方案：IP-Adapter 风格的 cross-attention 注入 vs. 当前的 context 拼接方案

---

## 附录

### 当前训练状态

| 项目 | 数值 |
|------|------|
| 数据集 | MSR-VTT 1000 clips |
| 分辨率 | 272×480，1s@16fps |
| 当前步数 | 2090 / 5000 |
| 当前 loss | ~0.13–0.30 |
| GPU | A100 40GB，显存占用 28.6GB |
| 已保存 checkpoints | step 500 / 1000 / 1500 / 2000（各 557MB） |

### 关键超参数

```toml
[model]
latent_height = 34, latent_width = 60, compressed_num_frames = 5
seq_len = 2550

[ttt]
mini_batch_size = 30
base_lr = 1.0
scan_checkpoint_group_size = 4

[training]
num_steps = 5000, warmup_steps = 200
lr_ttt = 1e-4, lr_base = 1e-5
global_batch_size = 8, dtype = bfloat16
```

### 参考文献

1. Sun, Y., et al. *Learning to (Learn at Test Time): RNNs with Expressive Hidden States.* arXiv:2407.04620, 2024.
2. Dalal, G., et al. *Test-Time Training on Video with TTT-Linear.* (ttt-video-dit), 2024.
3. Wan Team (Alibaba). *Wan: Open and Advanced Large-Scale Video Generative Models.* 2025.
4. Ruiz, N., et al. *DreamBooth: Fine Tuning Text-to-Image Diffusion Models for Subject-Driven Generation.* CVPR 2023.
5. Ye, H., et al. *IP-Adapter: Text Compatible Image Prompt Adapter for Text-to-Image Diffusion Models.* arXiv:2308.06721, 2023.
